{
  "name": "@checkout/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "tsx --test test/config.test.ts test/foundation-stack.test.ts test/release-stacks.test.ts",
    "synth": "cdk synth --quiet",
    "typecheck": "tsc --noEmit",
    "verify": "pnpm run build && pnpm run test && pnpm run synth"
  },
  "dependencies": {
    "aws-cdk-lib": "2.265.0",
    "constructs": "10.8.1"
  },
  "devDependencies": {
    "aws-cdk": "2.1136.0"
  }
}
